# Shared

Shared application-level artifacts for the unified control center.

Examples:

- stable app metadata contracts
- job status enums
- UI-facing compatibility states
- export/import conventions for managed config assets
